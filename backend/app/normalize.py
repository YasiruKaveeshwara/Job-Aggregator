"""
Text cleaning and role-keyword matching.

Takes a RawJobPosting (from any scraper) and produces the cleaned fields
for the canonical Job row: stripped whitespace, normalised company name,
parsed salary, and role-keyword match.

Implemented in Phase 4.
"""
