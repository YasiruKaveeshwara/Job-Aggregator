# Desktop Application Package

This folder contains the build configuration and scripts for packaging the Job Aggregator application as a native Windows desktop `.exe`.

## Assets Requirement
- `icon.ico`: A `.ico` image file (at least 256x256 resolution) should be supplied in this directory to serve as the application icon.
